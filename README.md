# Authflow
A NextJS Authentication

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Set the `MONGODB_URI` in [.env](.env)
3. Set the `JWT_SECRET` in [.env](.env) 
4. Run the app:
   `npm run dev`
